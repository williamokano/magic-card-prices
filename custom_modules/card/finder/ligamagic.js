var request = require('request');
var cheerio = require('cheerio');

/* Resource for ligamagic */
module.exports = {
  find : function(cardname) {
    // Add some way to get the card with this resource
    return null;
  }
}
